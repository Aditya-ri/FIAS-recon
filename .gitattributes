# Tell Git to handle all text files automatically
* text=auto

# But for these specific files, ALWAYS use Linux line endings (LF)
# This is the "automatic dos2unix"
*.py  text eol=lf
*.sh  text eol=lf
*.json text eol=lf
